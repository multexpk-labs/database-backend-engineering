# Query Performance

Investigate slow queries with evidence: capture the query and parameters, run EXPLAIN, inspect indexes and row estimates, measure before/after, and verify application behavior.

Common causes include missing indexes, poor composite indexes, large scans, inefficient joins, N+1 queries, unbounded results, and expensive sorting.