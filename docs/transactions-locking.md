# Transactions, Locking & Deadlocks

Study ACID, isolation levels, row/gap locks, lock wait timeouts, and deadlock detection.

Keep transactions short, acquire resources consistently where possible, and retry only operations that are safe to retry.