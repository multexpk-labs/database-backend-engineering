# Data Modeling

Design entities, keys, relationships, constraints, lifecycle rules, and indexes from real access patterns. Normalize first; denormalize only when measured requirements justify it.

Keep public examples synthetic.